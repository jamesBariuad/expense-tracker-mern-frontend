import React from 'react'

const DisplayAccounts = ({accounts}) => {
    // const account = accounts.map(account =>{ return(account._id)})
    console.log(accounts)
  return (
    <div>
      {/* {account} */}
    </div>
  )
}

export default DisplayAccounts